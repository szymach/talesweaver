{% extends 'displayBase.html.twig' %}

{% block body %}
    <h1 style="margin-bottom: 30px;" class="text-center">{{ scene.title }}</h1>
    <div class="row">
        <div class="col-sm col-sm-10 offset-sm-1 col-md-8 offset-md-2">
            <div class="display scene">
                {{ scene.text|raw }}
            </div>
        </div>
    </div>
{% endblock %}
