<li>
    <i class="fa fa-2x fa-bookmark-o visible-xs visible-sm js-trigger-sidelist-mobile logo-icon cursor-pointer"
       title="{{ 'scene.tabs.events'|trans }}"
    ></i>
    <span class="h4">
        <button class="btn btn-xs btn-default js-list-toggle"
                data-list-url="{{ path('event_list', { id: sceneId, page: 1 }) }}"
                title="{{ 'crud.list'|trans }}"
        >
            <span class="fa fa-list"></span>
        </button>
        <button class="btn btn-success btn-xs js-list-toggle"
                title="{{ 'event.actions.create'|trans }}"
                data-list-url="{{ path('event_options', { id: sceneId }) }}"
        >
            <span class="fa fa-plus"></span>
        </button>
        <span>{{ 'scene.tabs.events'|trans }}</span>
    </span>
    <span class="js-list-container"></span>
</li>
