{% extends 'base.html.twig' %}

{% form_theme form 'form/formTheme.html.twig' %}

{% block seoTitle 'security.profile.header'|trans %}
{% block body %}
    {{ form_start(form) }}
        {{ form_rest(form) }}
        <div class="form-group row">
            <div class="col-sm text-right">
                <button class="btn btn-success">{{ 'security.profile.submit'|trans }}</button>
            </div>
        </div>
    {{ form_end(form) }}
{% endblock %}
