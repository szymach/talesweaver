{% extends 'administration/base.html.twig' %}

{% trans_default_domain 'administration' %}

{% block seoTitle 'author.title'|trans %}
{% block body %}
    {% include 'administration/partial/dataList.html.twig' with {
        headers: authors.headers,
        rows: authors.paginator,
        actions: [
            {
                button: 'primary',
                path: 'admin_resend_activation_code',
                title: 'author.resend_activation_code',
                icon: 'mail-forward'
            }
        ],
        noResults: { header: 'author.no_results' }
    } only %}
{% endblock %}
