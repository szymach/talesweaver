<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-label="{{ 'modal.close' | trans }}">
        <span aria-hidden="true">&times;</span>
    </button>
    <h4 class="modal-title">{{ location.name }}</h4>
</div>
<div class="modal-body">
    <div class="container-fluid">
        <div class="row">
            {% if location.avatar %}
                <div class="col-xs-12 col-md-6">
                    <img class="img-responsive center-block" width="300" src="{{ fsi_file_url(location.avatar) }}" />
                </div>
            {% endif %}
            <div class="col-xs-12{% if location.avatar %} col-md-6{% endif %}">
                {{ location.description|raw }}
            </div>
        </div>
        {% if location.characters | length or location.items | length %}
            <div class="row">
                {% if location.characters | length %}
                    <div class="col-xs-4">
                        <h5>{{ 'location.characters' | trans }}</h5>
                        <hr/>
                        <ul class="list-unstyled">
                            {% for item in location.characters %}
                                <li>{{ location.name }}</li>
                            {% endfor %}
                        </ul>
                    </div>
                {% endif %}
                {% if location.items | length %}
                    <div class="col-xs-4">
                        <h5>{{ 'location.items' | trans }}</h5>
                        <hr/>
                        <ul class="list-unstyled">
                            {% for item in location.items %}
                                <li>{{ item.name }}</li>
                            {% endfor %}
                        </ul>
                    </div>
                {% endif %}
            </div>
        {% endif %}
    </div>
</div>
<div class="modal-footer">
    <button type="button" class="btn btn-default" data-dismiss="modal">
        {{- 'modal.close' | trans -}}
    </button>
</div>
