<table class="table">
    <thead>
        <tr>
            <th>{{ 'character.name'|trans }}</th>
            <th>{{ 'crud.actions'|trans }}</th>
        </tr>
    </thead>
    <tbody>
    {% for character in characters %}
        <tr>
            <td>{{ character }}</td>
            <td>
                <button class="modal-load-edit-form btn btn-primary"
                    data-edit-form-url="{{ path('app_character_edit', { id: character.id }) }}"
                    title="{{ 'crud.edit' | trans }}"
                >
                    <span class="fa fa-edit"></span>
                </button>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>
