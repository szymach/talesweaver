{% extends 'base.html.twig' %}

{% datagrid_theme datagrid 'crud/datagrid.html.twig' %}

{% block body %}
    {% block upper_buttons -%}
        <div>
            <a class="btn btn-sm btn-success" href="{{ path('crud_create', { elementName: element }) }}">
                {{- 'crud.list.create' | trans -}}
            </a>
        </div>
    {%- endblock upper_buttons %}
    {{ datagrid_widget(datagrid) }}
    {#{% block lower_buttons -%}
        <div>
            <a class="btn btn-success" href="{{ path('crud_create', { elementName: element }) }}">
                {{- 'crud.list.create' | trans -}}
            </a>
        </div>
    {%- endblock lower_buttons %}#}
{% endblock %}
