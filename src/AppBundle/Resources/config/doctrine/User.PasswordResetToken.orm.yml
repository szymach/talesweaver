---
AppBundle\Entity\User\PasswordResetToken:
    type: entity
    id:
        id:
            type: integer
            generator: { strategy: AUTO }
    fields:
        value: ~
        createdAt:
            type: datetime_immutable
    manyToOne:
        user:
            targetEntity: AppBundle\Entity\User
            inversedBy: passwordResetTokens
            joinColumn:
                onDelete: "CASCADE"
