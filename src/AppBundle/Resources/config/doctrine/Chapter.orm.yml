---
AppBundle\Entity\Chapter:
    type: entity
    repositoryClass: AppBundle\Repository\Doctrine\ChapterRepository
    id:
        id:
            type: uuid
            generator: { strategy: NONE }
    fsi:
        translatable:
            locale: locale
            fields:
                title:
                    mappedBy: translations
                    targetField: title
    fields:
        createdAt:
            type: datetime
        updatedAt:
            type: datetime
            nullable: true
    oneToMany:
        scenes:
            targetEntity: AppBundle\Entity\Scene
            mappedBy: chapter
            cascade: [persist]
        translations:
            targetEntity: AppBundle\Entity\Translation\ChapterTranslation
            mappedBy: chapter
            indexBy: locale
            cascade: [persist, remove]
    manyToMany:
        characters:
            targetEntity: AppBundle\Entity\Character
            inversedBy: chapters
    manyToOne:
        book:
            targetEntity: AppBundle\Entity\Book
            inversedBy: chapters
            joinColumn:
                onDelete: "CASCADE"
        createdBy:
            targetEntity:  AppBundle\Entity\User
            joinColumn:
                onDelete: CASCADE
